# Handoff Notes

The previous implementation pass stopped after wiring a basic list view. The
next agent should focus on the pure logic contract first:

1. migration;
2. risk score;
3. queue filtering;
4. decision application;
5. policy summary.

After the acceptance tests pass, update the UI labels enough that a human can
see risk score, status, and the local-only policy boundary.

Do not use archived guidance as implementation authority.

